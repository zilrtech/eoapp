<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dev.university.eoapp">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.AddNewOffer"></activity>
        <activity
            android:name=".AddOfferToCategory"
            android:label="@string/title_activity_add_offer_to_category" />
        <activity android:name=".activities.RegisterActivity" />
        <activity android:name=".activities.user_profile" />
        <activity android:name=".activities.updateActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.ProfileActivity" />
        <activity android:name=".activities.HomeActivity" />
        <activity android:name=".activities.splash" />
        <activity android:name=".activities.ShopActivity" />
        <activity android:name=".activities.OffersActivity" />
        <activity android:name=".activities.category_offers" />
        <activity android:name=".activities.addevent" />
        <activity android:name=".activities.show_my_events" />
        <activity android:name=".activities.event_details" />
        <activity android:name=".activities.event_offersActivity" />
        <activity android:name=".activities.ChatActivity" />
        <activity android:name=".activities.ChatlistActivity" />
        <activity android:name=".activities.beginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>